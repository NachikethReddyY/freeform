# FreeForm feature map

Source review refreshed 25 September 2026 against source, focused tests, and disposable-board browser checks. This pass added native technical nodes, five Arrange choices, a bounded selection-to-Mermaid command, reviewed paste cards, a server-owned catalog, stricter SQL import, AI Stop body cancellation, Focus mode, a compact Layers hierarchy, an SDK minimap, and a presentation laser-trail refinement. Safari checked keyboard node labeling, tree/radial/compact Arrange, Markdown card insertion/reload, Focus enter/exit, Layers selection/centering, minimap navigation, rectangle/ellipse Size with independent stroke width, sharp/rounded edges, collision-free title/controls at 720px and 390px, a nonblank two-frame first slide, Next/Previous, and a direct short fading blue laser trail; Helium saved light/dark first-slide screenshots. A clean two-tab Safari remote connected, advanced both views to 2/2 and left the presenter stage visible after returning. A separate three-frame Safari board stayed nonblank during and after two quick Next actions. Remote touchpad movement and broader transition animation appearance remain unverified. Other claims retain the limits shown below. The source brief is `docs/ideas.md` in the sibling project (local source brief, not distributed here): its original 44 numbered themes contain **749 list ideas** plus the two narrative canvas-as-browser/IDE concepts. A new section 45 records the user's personal technical-canvas priorities separately. In the original-theme matrix below, **every idea in a source line interval is absent as a dedicated feature unless the row explicitly names it as proven or partial**. “Partial” includes a usable generic canvas primitive, but does not claim the named product or workflow is implemented.

## Evidence scorecard

| Dimension | Status | Sample and counting method | Evidence | Confidence and limit |
| --- | --- | --- | --- | --- |
| Local whiteboard and files | Proven core, partial parity | One implemented room/dashboard/asset path; earlier native JSON/`.tldr` imports, two-page and embedded-PNG reload, page/selection PNG/SVG, transparency and draw-font SVG; a native `.tldr` export/import/reload kept three labeled shapes | `../client/features/portability/BoardFiles.tsx`, `../client/features/portability/nativeBoard.ts`, `../worker/TldrawDurableObject.ts`, local browser screenshots `board-files-matrix-import-page2.png` and `board-files-matrix-import-media-reload.png` (not distributed) | High for named flows and one small native round trip; external linked media, other fonts, scale and larger files remain unverified. Isolated two-client SDK/WebSocket collaboration passed, but a browser cursor/UI pass has not. |
| Diagramming and code interchange | Partial | All 36 ideas in theme 2; eight editable starters (five prior plus three developer starters); Safari checked a five-node graph through Radial and Compact, including bound arrows, camera fit and separate compact branch captions; SQL parser rejects unsupported constraints and preserves composite-key order in 11 focused tests | `sibling docs/ideas.md:142-181`, `../client/features/diagrams/layout.ts`, `../shared/sqlDiagram.ts`, `../client/features/paste/diagramPaste.ts`, `../bug.md` B15 | High for named small graphs and parser contracts; broader source formats, dense boards and broad viewports remain unverified. |
| AI and agents | Partial | Themes 4–5 (62 list ideas); one mock-provider browser path loaded a model, answered Ask, returned a Draw preview, inserted three native shapes, and retained them after reload; MCP smoke covers proposal transport with no model call; 13 focused gateway tests include timeout/rate-limit/interrupted responses and Stop cancellation of an in-progress provider body | `sibling docs/ideas.md:219-306`, `../client/features/ai/AIChatPanel.tsx`, `../worker/aiGateway.ts`, `../mcp/server.ts`, `../mcp/README.md` | High for the mocked local path and focused cancellation tests; actual Ollama/LM Studio or hosted-provider inference and live Stop are unverified, and this is not an executable agent workflow builder. |
| Local data placement | Server-owned catalog; other stores remain split | An owner catalog merge/persistence test set and isolated Worker register/sync/delete/restart path; source audit of rooms, assets, thumbnails and personal blocks | `../shared/boardCatalog.ts`, `../worker/catalogStore.ts`, `../worker/AuthDurableObject.ts`, `../client/features/boards/boardIndex.ts`, `../worker/TldrawDurableObject.ts` | High for catalog code and isolated persistence; live multi-browser catalog UI, all-data migration and one unified database remain unverified/absent. |
| Presentation | First slide, direct trail, remote return and rapid Next pass | One frame-based presenter, tab-filling by default with explicit browser fullscreen. Earlier organizer checks cover reorder/undo/reload and rename cancel. Helium captured light/dark slides; Safari showed nonblank content, Next/Previous and a blue trail. A clean two-tab Safari remote connected and advanced both views to 2/2; the presenter stage remained visible on return. On a separate three-frame board, two quick Next actions kept Slide 3 visible during and after the camera move. Auth focus recheck keeps the same-owner board mounted while hidden/inert. Auth tests pass 8/8 and presentation tests 29/29. | `../client/features/auth/AuthGate.tsx`, `../client/features/presentation/presentationStage.tsx`, local `.evidence/presentation-complete/light-first.png` and `dark-first.png` | High for the sampled slides, trail, rapid navigation and one remote return. Touchpad and broader transition appearance remain visually unverified. Speaker notes, live audience sharing and PPTX/PDF slides remain absent. |
| Search and board navigation | Proven in a small sample | One disposable two-page board; text match/no match, Clear, Escape, ArrowUp/Down/Enter selection and cross-page select/center; one board hard reload | `../client/features/search/SearchPanel.tsx`, `../worker/navigation.ts`, local browser screenshots `search-keyboard-active.jpg` and `deep-link-reload-zen.png` (not distributed) | High for the sampled paths; visible results cap at 100, large-board speed and broader shape types are unmeasured. |
| Wider idea brief | Mostly absent | All 44 themes / 749 list ideas, using each theme's line interval and the explicit exceptions below | `sibling docs/ideas.md:119-1313`, `../docs/FEATURE_CHECKLIST.md:79-107` | High for source coverage, medium for exhaustive runtime claims; this is a repository scan, not a manual test of every native tldraw control. |

## Added section 45: personal technical canvas

These statuses cover the ten priority rows added to the sibling brief, counted once each. “Partial” means the named workflow has a bounded slice; it is not a claim of full technical-node semantics or live developer integrations.

| Priority | Status and observed slice | Evidence | Confidence and limit |
| --- | --- | --- | --- |
| Smart connectors | Partial: four-direction native bound node creation and direct label editing through canvas controls or Cmd/Ctrl+K; Safari API-stack insertion, node movement and hard reload kept labeled arrows attached | `../client/features/diagrams/connectedNodeControls.tsx`; `../client/features/commandPalette/CommandPalette.tsx`; focused Editor binding test | High for the checked paths; edge dragging/ports and obstacle routing are absent. |
| Diagram mode | Partial: horizontal, vertical, tree, radial and compact Arrange for eligible selected native nodes. Safari Cmd+K Radial/Compact on a five-node graph retained bound arrows and fit beneath the toolbar; a revised compact lane gap visibly separated Yes/No captions. Layout subset passes 17/17, with a failed-first no-op camera-fit regression now passing. | `../client/features/diagrams/layout.ts`; `../client/features/diagrams/layout.test.ts`; `../client/features/commandPalette/commands.test.ts` | High for the sampled graph and test rules; radial is capped at 16 connected nodes, tree rejects cycles/shared descendants, and large-board performance remains open. |
| Technical nodes | Partial: six editable native API/database/service/queue/function/cloud shapes plus API/data/request starters | `../client/features/diagrams/technicalNodes.ts`; `../client/features/diagrams/library/DiagramLibrarySection.tsx`; focused library tests | High for native editability in tests; typed fields, executable behavior and live status are absent. |
| Mermaid round trip | Partial: bounded editable Mermaid flowchart import and selected-native-diagram text export | `../client/features/diagrams/mermaid.ts`; `../client/features/diagrams/mermaidExport.ts`; `../client/features/diagrams/nativeBindings.test.ts` | High for parsed selection export test; complex native styles, unsupported shapes and general round-trip fidelity are not preserved. Board export remains native FreeForm/tldraw `.tldr`. |
| Universal paste | Partial: four diagram previews plus bounded Markdown, code, JSON and HTTP URL card proposals; matching `text/plain` + `text/uri-list` URLs also qualify in focused tests; Safari Markdown Add/reload | `../client/features/paste/`; 24/24 paste tests and native round-trip tests | High for bounded classification and one browser Markdown path; the URI-list URL variant has focused tests only. Mixed/rich clipboard, all code languages and broad viewports remain unverified. |
| Markdown/code cards | Partial: reviewed preview and editable native shape; safe render, original source metadata, code highlighting in preview | `../client/features/paste/CardPastePanel.tsx`; `../client/features/paste/nativeCard.ts`; `.evidence/postplan-partial-proof/` | High for focused tests and new Safari Markdown card; on-canvas inline emphasis is flattened for readability and is retained only in source/preview. Live URL unfurling is separate. |
| Search/minimap/layers | Partial: board-content search, an SDK minimap toggle in the zoom controls, and a compact current-page native shape hierarchy | `../client/features/search/SearchPanel.tsx`; `../client/features/layers/`; prior cross-page search pass; Safari minimap and four-row Layers passes; three focused Layers tests | High for sampled search, minimap navigation and selecting/centering Slide 1 from Layers. Nested row selection, large-board and narrow-viewport navigation remain unmeasured; this is not a full Figma-style object manager. |
| Local-first portability | Partial: local room persistence, server-owned catalog with browser fallback, native `.tldr` export/import | `../client/features/portability/BoardFiles.tsx`; `../worker/catalogStore.ts`; prior multi-page/asset pass and three-shape `.tldr` round trip | High for named small file and isolated catalog paths; thumbnails/personal blocks remain browser-local and large-file fidelity remains unverified. |
| Version history | Absent: editor undo is not a named restorable snapshot | `../client/pages/Room.tsx` | High from source scope. |
| Presentation/focus | Partial: ordered frame slides, tab-filling stage with optional browser fullscreen, scoped remote, light/dark first-slide captures, Safari first-slide/Next/Previous, a rapid two-step Next pass and a direct blue trail; Cmd/Ctrl+K Focus hid editor and FreeForm chrome, then restored controls | `../client/features/auth/AuthGate.tsx`; `../client/features/presentation/`; `../client/features/commandPalette/`; auth 8/8, presentation 29/29, palette 9/9 and Safari passes | High for the sampled slides, trail, Focus and a clean two-tab remote Connected/Next/return pass. Touchpad and broader transition appearance remain visually unverified. |

## Specific requests from the chat

This table names the remaining requests that can be mistaken for complete features because a nearby canvas primitive exists. The statuses are based on source inspection and the focused checks named above; this pass did not repeat every browser flow.

| Request | Current status | What exists now | What is still missing or unverified |
| --- | --- | --- | --- |
| Native `.tldr` board file | Implemented for tested scope | Board Files downloads and imports native editable tldraw records; a three-shape round trip and an earlier two-page/embedded-asset import survived reload. | Large boards and external media fidelity have not been checked. Mermaid text export is a separate selected-diagram command, not the board format. |
| Four-direction quick connect | Implemented for tested scope | A selected native node can create a bound neighbor above, below, left or right from the canvas controls or Cmd/Ctrl+K, then immediately edit its label (`../client/features/diagrams/connectedNode.ts`). A Safari palette flow typed into the new node without a second click. | Dragging from ports into a node picker and obstacle-aware routing remain absent. |
| Command palette and keyboard-first diagrams | Partial | Cmd/Ctrl+K offers Create node, six technical node choices, four bound Connect directions, and horizontal/vertical/tree/radial/compact Arrange when eligible (`../client/features/commandPalette/`). Safari typed into a newly connected node and applied tree/radial/compact Arrange to selected graphs. | Broad diagram traversal/manipulation without a mouse remains absent; only a five-node radial/compact visual sample was checked. |
| Reusable blocks and templates | Implemented for native diagrams | Save and reinsert a native selection, retaining bindings; eight starter diagrams (`../client/features/diagrams/library/`). | Parameterized or typed developer components remain absent. |
| SQL and OpenAPI imports | Implemented for bounded inputs | `CREATE TABLE` and OpenAPI 3.x JSON create reviewable, editable diagrams. SQL now rejects unrepresentable CHECK/UNIQUE/referential actions and non-primary targets, while keeping composite primary-key order; 11 focused parser tests pass (`../shared/sqlDiagram.ts`, `../client/features/diagrams/importers/openapi.ts`). | Full schemas, live API calls, database execution and linked data are absent. The newest rejection cases have focused tests, not separate browser screenshots. |
| AI chat with model selection | Partial | Ask and reviewed Draw work with a configurable loopback Ollama or compatible endpoint; a deterministic mock browser pass inserted three native shapes. The panel now renders inside the auth-gated editor container and still opened in Safari. Stop propagates to the provider call and response body in focused gateway tests (`../client/features/ai/AIChatPanel.tsx`, `../worker/aiGateway.ts`). | Real Ollama/LM Studio/hosted inference, live-provider Stop and autonomous multistep edits have not been verified. The portal correction is not a full auth audit. |
| Local agent MCP access | Implemented for reviewed proposals | The MCP server reads bounded board context and queues native, SQL and OpenAPI proposals; browser acceptance is required (`../mcp/server.ts`). | General canvas manipulation tools and workflow execution are absent. |
| URL cards and live architecture | Partial | A safe HTTP URL can become a reviewed native link card without fetching it; a matching `text/plain` + `text/uri-list` clipboard pair is accepted in 24/24 focused paste tests. Native bookmark unfurling is also registered (`../client/features/paste/cardPaste.ts`, `../client/pages/Room.tsx`). | The URI-list path lacks browser proof. Tailored GitHub/docs/YouTube cards, localhost service status, routes and logs are absent. |
| Presentation remote | Partial | A scoped same-origin remote synchronized navigation and laser toggle between Safari and Helium in an isolated test. After retaining the same-owner board through auth focus rechecks, a clean two-tab Safari pass showed Connected, remote Next advanced both views to 2/2, and the presenter stage remained visible on return. The presenter host reports no active slide and ignores commands while auth recheck is pending. Light/dark slides and a direct Safari laser gesture have visual proof. | The pending-command guard has source review rather than a direct browser pass. Remote touchpad movement, remote-driven transition appearance and broader remote acceptance remain unverified. |
| Executable visual workflows | Absent | Native arrows and editable diagram starters can sketch a workflow. | Typed ports, runner, state, results, retries and agent-step orchestration. |
| Single local database | Absent | Catalog/collections now sync to the local owner's SQLite-backed Durable Object, rooms use separate Durable Object SQLite, assets R2 emulation, thumbnails IndexedDB, and personal blocks browser storage. | A unified all-data model, complete migration and browser proof across profiles. |
| Version snapshots | Absent | Native editor undo is available. | Named snapshots, restore points and history browsing. |

## Existing and previously proven

- **Canvas base:** Native shapes, free draw, text, notes, arrows, frames, pan/zoom and edit history through the installed tldraw editor; local sync and uploaded media persistence survived host and isolated Docker restart tests. FreeForm customizes Excalifont, colors, strokes and an arrow curve while keeping native shape records (`../client/pages/Room.tsx:47-75,182-213,262-307`; `../client/editor/excalidrawShapes/README.md:1-44`; `../docs/FEATURE_CHECKLIST.md:9-13`).
- **Board organization:** Create/open, inline rename, search by name, collections, two-board bulk move, Copy link, recoverable Trash and browser-local previews have reported browser proof. A later Safari responsive check shows the editor's full board title at 720px and a deliberately ellipsized title at 390px; top controls stay separate at both widths (`.evidence/postplan-partial-proof/chrome-720-safari.png`, `chrome-390-safari.png`, local). Catalog metadata now syncs to the local owner's SQLite store with a browser cache; isolated API/restart proof covers it, while wider UI/profile proof is pending (`../client/features/boards/boardIndex.ts`; `../worker/catalogStore.ts`).
- **Editable source diagrams:** Bounded Mermaid flowchart, SQL `CREATE TABLE`, and OpenAPI 3.x JSON inputs preview and insert native tldraw shapes; unsupported source is rejected with an error. The SQL parser's 11 focused cases now explicitly reject unrepresentable constraints and preserve composite primary-key order. A connected Codex/Pi session can read a bounded board summary and queue native, SQL ERD, or OpenAPI endpoint-map proposals for browser review before insertion. Recognized diagram code and short arrow chains pasted on the canvas open the review UI; ordinary/native/file paste stays with tldraw (`../mcp/README.md`; `../shared/sqlDiagram.ts`; `../client/features/diagrams/DiagramProposalPanel.tsx`).
- **Optional in-app AI chat:** A loopback Ollama or OpenAI-compatible endpoint can be configured in the browser, with model loading/manual choice, Ask and Draw modes, optional bounded current-page context, validated Draw preview and explicit Add. Endpoint/model settings use tab session storage; the API key stays in tab memory and out of board records. A mock-provider browser pass covered Load models → Ask → Draw → Add → reload with three native shapes. Real Ollama/LM Studio inference has not been verified (`../client/features/ai/AIChatPanel.tsx`; `../worker/aiGateway.ts`).
- **Basic slide presentation:** Top-level frames are slides in explicit saved order, with spatial order as the fallback. Start/exit, keyboard navigation, fit, tab-filling stage, optional browser fullscreen and restoration have focused tests; light/dark first-slide captures, a Safari two-frame nonblank first slide with Next/Previous, and a directly drawn short fading blue trail have visual proof. In a clean two-tab Safari pass, the remote displayed Connected, advanced both views to 2/2 and the presenter stage stayed visible on return. A separate three-frame Safari board showed Slide 3 during and after two quick Next actions. The auth focus recheck retains a same-owner mounted board while hidden/inert, then unmasks it after validation; invalid or different-owner sessions unmount it. Touchpad movement and broader transition appearance remain unverified (`../client/features/auth/AuthGate.tsx`; `../client/features/presentation/`; `../docs/FEATURE_CHECKLIST.md:55-61`).
- **Tool discovery:** Cmd/Ctrl+K searches a curated subset of native actions/tools and adds technical-node creation, bound Connect directions, eligible horizontal/vertical/tree/radial/compact Arrange and Focus mode. Safari verified direct typing into a new connected node, tree/radial/compact Arrange, and Focus enter/exit with editor and FreeForm chrome hidden then restored; palette tests pass 9/9. The zoom-control minimap was closed by default; in a Safari two-frame board its toggle opened the SDK overview, clicking the left frame changed the visible slide and camera URL, and Close removed the overview. A compact Layers panel lists current-page native shapes; Safari selected Slide 1 from four rows and centered the frame, and three focused tests cover hierarchy/selection (`../client/features/commandPalette/commands.ts`; `../client/features/layers/`; `../client/pages/Room.tsx`).
- **Faster diagram construction:** Selecting one supported node exposes four icon controls that add an editable connected rectangle with native arrow binding. New connections enter tldraw's label-editing state and focus the mounted text editor; a Zen browser check typed `Review` directly into the new node and hard reload retained the bound Idea → Review pair. Eight native-record starters, including new API stack, data model, and request flow examples, and saved personal blocks are available in Diagrams; bounded Arrange lays out selected connected flow nodes while protecting authored Sequence lanes. Browser checks cover Flowchart Arrange/undo/reload, polished Sequence insertion/reload with 21 native shapes in the room API, and personal-block save/reload/reinsertion. Safari's earlier lost page came from a hidden automation tab with zero animation frames, documented as F15 (`../client/features/diagrams/connectedNodeControls.tsx`; `../client/features/diagrams/connectedNode.ts`; `../client/features/diagrams/library/templates.ts`; `../client/features/diagrams/layout.ts`; local app-only screenshots `flow-arrange-panel-clear-app.jpg`, `sequence-zen-reload-app.jpg` and `connected-label-panel-app.jpg`, not distributed).
- **Portable files:** Board Files downloads a native FreeForm/tldraw `.tldr` JSON document and current-page/selection PNG or SVG; import validates before creating a new local room. A browser round trip downloaded a 4,825-byte, three-shape `.tldr` and imported it into a new room; Browser, API and GET /users labels remained after hard reload. The earlier browser matrix checked native JSON and `.tldr` imports, two pages, an embedded PNG after reload, all page/selection PNG/SVG downloads, transparency, draw-font SVG embedding, and invalid import without source-board mutation. Excalidraw v2 import remains available and a bounded fixture converted three elements and skipped one image. A prior version also exported a parseable three-element/one-binding file; the Files export control was removed (`../client/features/portability/README.md:1-7`; `../client/features/portability/excalidraw/README.md:3-15`; local browser screenshot `board-files-matrix-import-media-reload.png` (not distributed)).
- **Slide organization and compact controls:** Slides previews, renames, jumps and reorders top-level frames; browser checks covered reorder/undo/reload/fullscreen/Escape/rename cancel. The 720px toolbar and 1016px selected card were checked without overlap; independent geometry Stroke/Background presets, custom picker, transparent fill, SVG/PNG and reload passed on a disposable rounded rectangle (`../client/editor/excalidrawShapes/FreeformBackgroundPicker.tsx`; local screenshot `independent-colors-app.jpg`, not distributed). S→XL visibly changed a selected rectangle label and remained after reload. Safari checked selected rectangle and ellipse text Size while a separately chosen stroke width stayed fixed, plus sharp/rounded rectangle edges. Excalifont bold is visibly heavier in the editor, SVG, selected PNG, and a text-only dashboard thumbnail; wider responsive/action coverage remains open.

## Partial and absent ideas, covering the complete brief

The **Absent remainder** column is an explicit classification of every other list item in that theme; it is not a claim that a person cannot sketch the subject using generic shapes. No specialist tool is present unless the row says otherwise.

| Theme and source lines | Proven or partial exceptions | Absent remainder |
| --- | --- | --- |
| 1. Whiteboarding & collaboration, 119–141 | Basic whiteboard, sticky notes, eight diagram starters, reusable personal blocks and ordered local presentation exist; isolated two-client SDK/WebSocket drawing/move/upload/restart proof passed, but browser cursor/UI proof is pending. | Dedicated comments, voting, clustering, facilitation, workshop templates, access roles and live audience controls. |
| 2. Diagramming, 142–181 | Bounded Mermaid flowchart, SQL schema and OpenAPI JSON imports produce editable native diagrams. SQL rejects unrepresented constraints instead of inventing diagram semantics. Selected native nodes can export bounded Mermaid text; four-direction bound creation, horizontal/vertical/tree/radial/compact Arrange, six technical node presets, eight starters, and bounded `.excalidraw` import exist. Safari sampled radial/compact on five nodes. Native board export uses FreeForm/tldraw `.tldr`. | Specialist mind/concept-map, UML/class/sequence/state/activity, full ER/database, network/cloud/C4, BPMN, journey, sitemap, org/family, decision/dependency/lineage/API, threat, circuit/logic/math/biology/chemistry **editors and semantics**. |
| 3. Visual programming, 185–215 | Canvas nodes/arrows are a generic visual foundation only. | All listed executable scripting, language, automation, event, rule, game and bot workflow systems. |
| 4. AI workflow builders, 219–248 | Generic diagram proposals can depict a pipeline. | All listed executable LLM, RAG, embedding, data, image/audio, OCR, evaluation, routing, local-model and ML pipelines. |
| 5. AI agents, 252–306 | Local MCP board read and reviewable native/SQL/OpenAPI proposals are proven. In-app Ask/Draw, model selection, optional bounded page context and explicit diagram acceptance passed one mock-provider browser flow; focused tests now cover Stop cancellation through provider-body reads. No real-model inference is claimed. | Agent orchestration, traces, memory, token/context views, debugger, visual MCP client and sketch/code/screenshot transformations. |
| 6. Codebase tools, 312–350 | A code graph can be drawn manually. | Every listed repository, import/call/type/route, history/PR, coverage, ownership and code-analysis explorer. |
| 7. Git / GitHub tools, 375–392 | Generic canvas only. | All listed Git, PR, issue, release and contributor functions. |
| 8. Database tools, 396–420 | ERD/data-model starters and bounded pasted `CREATE TABLE` → editable table/foreign-key diagram import and MCP proposal exist. This parser does not connect to or execute against a database. | Live schema-aware editing, SQL browser/planner, migrations, lineage and every named database integration. |
| 9. API development, 442–461 | API-stack/request-flow starters and bounded pasted OpenAPI 3.x JSON paths → editable endpoint map import and MCP proposal exist. | GraphQL, API testing, auth, request/response and event workbenches, and full OpenAPI schema/behavior coverage. |
| 10. DevOps, 465–490 | Architecture boxes/arrows can be drawn manually. | Every live CI/CD, Docker, Kubernetes, IaC, cloud and environment integration. |
| 11. Developer environments, 494–514 | Generic canvas only. | All listed process, port, terminal, preview, service, build, test and log nodes. |
| 12. Debugging, 535–555 | Investigation sketches can be made manually. | Every listed live trace, stack, network, error, performance and incident tool. |
| 13. Defensive security, 573–594 | Threat boundaries can be drawn manually. | STRIDE, IAM, vulnerability, SBOM, audit and all source-backed security explorers. |
| 14. Data & analytics, 598–617 | Generic canvas only. | All listed SQL, BI, chart, dataset, funnel, cohort and metric tools. |
| 15. Machine learning, 621–641 | Generic canvas only. | All listed dataset, experiment, training, evaluation and MLOps integrations. |
| 16. Knowledge management, 645–665 | Native text, notes, bookmarks and arrows can form a manual knowledge map; the mind-map starter adds an editable initial layout. Bounded Markdown paste produces a reviewed editable card with its original source preserved. | Structured note/wiki graph, backlinks, search, import, timeline and knowledge-base views. |
| 17. Research, 669–688 | Images and notes can be placed manually; bookmarks unfurl through the Worker. A safe pasted HTTP URL can produce a reviewed native link card without fetching it (`../client/features/paste/cardPaste.ts`). | PDF annotation, citation/provenance graph, claim/evidence management, research agent and source comparison. |
| 18. Education, 694–722 | Board drawing and frame presentation support manual lessons. | Every listed interactive simulator, classroom workflow and subject-specific tool. |
| 19. Project management, 726–747 | Generic planning sketches only. | Kanban, sprint, task, dependency, timeline, capacity, risk and roadmap records/workflows. |
| 20. Product management, 751–767 | Generic discovery sketches only. | Dedicated feedback, story, prioritization, experiment and roadmap systems. |
| 21. UX/UI design, 771–789 | Basic wireframes, screenshot placement and storyboard frames can be made manually. | Component/state libraries, interactive prototyping, responsive and accessibility analysis, Figma organization. |
| 22. Website/app builders, 793–812 | Generic canvas only. | Every listed page, form, code, layout and responsive builder. |
| 23. Design systems, 816–827 | Native styling and custom HEX picker are usable drawing controls. | Token management, component variants, documentation and design-to-code workflows. |
| 24. Image tools, 831–849 | Images can be uploaded and arranged into a manual moodboard. | Dedicated annotation, organization, comparison, provenance and image-processing/AI tools. |
| 25. Video & media, 855–870 | Native video assets and frame arrangement offer a manual storyboard. | Shot/script linkage, media manager, editing and production workflow. |
| 26. Writing, 874–891 | Text, notes and arrows allow a manual plot map. | Dedicated writing, character, scene, timeline and calendar records. |
| 27. Interactive documentation, 895–910 | Editable diagrams, saved blocks, and notes can illustrate documents. | Live metadata, runbooks, documentation graph, tutorial and onboarding systems. |
| 28. File management, 914–928 | Board dashboard collections organize boards, not arbitrary files. | Spatial file explorer, tagging, duplicate analysis, downloads and dependency indexing. |
| 29. Personal productivity, 932–949 | Board collections and manual notes support loose planning. | Dedicated tasks, goals, habits, calendar, inbox, reading and study systems. |
| 30. Calendar & scheduling, 953–964 | Generic canvas only. | All listed calendars, availability and scheduling logic. |
| 31. Business operations, 968–985 | Manual process diagrams only. | All listed SOP, approval, CRM, sales, support and operational workflows. |
| 32. Event planning, 989–1001 | Manual event maps only. | Scheduling, seating, staff/equipment, venue and session tools. |
| 33. Spatial/floor planning, 1005–1019 | Shapes can represent a basic plan manually. | Scale-aware floor plans, objects, dimensions, seating and building navigation. |
| 34. Maps/travel, 1022–1035 | Images and notes can make a manual trip board. | Place/map data, routes, itinerary and collaboration workflows. |
| 35. Hardware/electronics, 1039–1052 | Manual block diagrams only. | Circuit/breadboard/PCB semantics, device/network integrations and rack tools. |
| 36. Homelab/infrastructure, 1056–1086 | Manual topology diagram only. | Live services, VMs, storage, backup, monitor and machine controls. |
| 37. Observability, 1090–1105 | Manual service map only. | Every listed telemetry, metrics, trace, log, alert and health integration. |
| 38. Simulations, 1108–1127 | Static drawings only. | All 16 listed executable simulations. |
| 39. Games/game development, 1131–1147 | Manual maps, trees and storyboards only. | Game-aware level, quest, dialogue, behavior, puzzle and asset tooling. |
| 40. Creative spatial apps, 1151–1179 | Generic infinite canvas is present. | All listed browser, IDE, OS, email, RSS, GitHub, API, search, file and community clients. |
| 41. Canvas-as-browser, 1183–1203 | Native bookmarks can represent links. | Live page/tab objects and cross-source relationships. |
| 42. Canvas-as-IDE, 1207–1227 | Generic canvas only. | Live code, test, database and browser widgets. |
| 43. Canvas-as-AI-OS, 1231–1266 | Reviewable MCP and in-app AI Draw proposals are small human-in-the-loop primitives. | The named agent/model/tool/file/terminal/browser/prompt/result/memory/task node roles as an executable system. |
| 44. Canvas-as-dev-control-plane, 1270–1310 | API-stack/data-model/request-flow starters, six editable technical-node presets and source-to-diagram imports make a static developer canvas. | All 12 listed live service/process/log/database/API/port/Docker/Git/CI/deployment/infrastructure/agent integrations. |

## Excalidraw parity and source reuse

The local Excalidraw checkout's [README at the inspected revision](https://github.com/excalidraw/excalidraw/blob/4850bf336fe0a8501af49d84deebc7fb56357cc2/README.md) lists its **free editor** surface: infinite canvas, hand-drawn style, dark mode, images, shape libraries, localization, PNG/SVG/clipboard and `.excalidraw` export, core shapes, binding, undo and zoom (sibling `README.md:52-69`). Its hosted app additionally advertises PWA/offline, encrypted real-time collaboration, local-first save and read-only share links (sibling `README.md:71-81`). [Excalidraw Plus](https://plus.excalidraw.com/) advertises cloud workspaces, advanced sharing, live presentations, extended collaboration and upgraded generative AI; those paid-service features are **not** provided by the local open-source checkout. This comparison is feature-level and does not imply FreeForm includes those features.

FreeForm covers much of the drawing base through tldraw, adds a small personal block library and bounded `.excalidraw` import, and has browser-checked native `.tldr`/PNG/SVG file actions. The Files panel no longer offers `.excalidraw` export. A local mock-provider check now covers in-app AI chat and a reviewed Draw insertion. Excalidraw parity remains partial: this is not its full library ecosystem or file fidelity, and localization, PWA/offline, encrypted sharing, read-only links, real-provider AI acceptance, and Plus-grade cloud workspace/live collaboration remain absent or unverified (`../README.md`; `../client/features/portability/excalidraw/README.md:11-15`).

**Copying boundary:** The local Excalidraw checkout is [MIT licensed](https://github.com/excalidraw/excalidraw/blob/4850bf336fe0a8501af49d84deebc7fb56357cc2/LICENSE); copied source must retain its copyright and license notice (sibling `LICENSE:1-20`). The current interchange adapter references its public v2 file format and says no Excalidraw source was copied (`../client/features/portability/excalidraw/README.md:15`). FreeForm's app `LICENSE.md` is MIT (`../LICENSE.md:1-9`), but installed `tldraw@5.4.2` points to the [tldraw license](https://github.com/tldraw/tldraw/blob/main/LICENSE.md), not MIT (installed tldraw 5.4.2 package `package.json:1-7` and `LICENSE.md:1`; package source is not distributed here). Connected-node creation cites an official tldraw example as a source of its native shape/binding flow (`../client/features/diagrams/connectedNode.ts:9-10`). The SDK works in development; [production requires a valid trial, hobby or commercial key](https://tldraw.dev/sdk-features/license-key), with the hobby watermark. Keep SDK code under its own license; Excalidraw Plus service features are not donor source.

## Next five user-visible gaps

The previous top four priorities—connected nodes, a starter/personal library, board files, and slide order—now have implementation and named browser proof above. These are the next gaps by diagramming value, UX impact, SDK fit and effort. New controls should remain in compact, contextual panels.

| Rank | Feature and minimum useful behavior | UX value | SDK fit | Effort / proof |
| --- | --- | --- | --- | --- |
| 1 | **Finish the remaining style states.** A selected rectangle's Size changes its label visibly, independent geometry Stroke/Background choices work, and Excalifont Bold is visibly heavier in selected PNG and a text-only dashboard thumbnail. Safari now also checked rectangle/ellipse label Size with a separately chosen stroke width held fixed and sharp/rounded rectangle edges. Test the remaining theme/mobile combinations. | Very high: every diagram depends on legible text. | Native geo text styling and local font theme (`../client/editor/excalidrawShapes/strokeControls.test.ts`; `../client/editor/fontTheme.ts`). | Medium-high for sampled shapes; broader viewport/theme coverage remains open. |
| 2 | **More connected diagram types.** The original five starters include a chronological Sequence example with invisible bound anchors, dashed headless lifelines and four message arrows; three developer starters now join them. Arrange has horizontal, vertical, tree, bounded radial and compact modes for eligible connected native nodes while protecting authored Sequence lanes. Add specialist mind-map, ERD, sequence and architecture editing semantics. | High: advances the user's diagramming goal beyond static templates. | Existing native bindings, reusable blocks and bounded layout (`../client/features/diagrams/native.ts`; `../client/features/diagrams/library/templates.ts`; `../client/features/diagrams/layout.ts`). | High for named earlier desktop checks, the 17/17 layout subset and 57/57 broader diagram suite. Flowchart/tree arrangement and panel-clear fit have browser proof; Safari also checked radial/compact on one five-node flow and rechecked separated Yes/No captions after a lane-gap correction. The API-stack starter has insertion, Arrange, node-move/reload and label-spacing captures; other developer starters have source/test evidence only. Large-board performance is unmeasured, and specialist editors remain absent. |
| 3 | **Expand board search after its first useful slice.** The compact top-right panel finds native text/geo/note/arrow labels across pages; ArrowUp/Down/Enter activates a result. Assess large-board behavior before widening the index. | High for larger study boards. | Native tldraw store/page navigation (`../client/features/search/searchIndex.ts`; `../client/features/search/SearchPanel.tsx`). | Medium. Disposable-board browser passes covered text result, page switch, camera focus, keyboard active/Enter, no-result state, Clear and Escape; large-board scale remains unmeasured. |
| 4 | **Real-provider and wider AI acceptance.** The chat panel now has model selection, Ask/Draw, optional page context and reviewed Add. Exercise actual Ollama/LM Studio and a compatible hosted endpoint, failures/retries and constrained viewports. | High for confidence in the new local workflow. | Existing bounded gateway and native proposal contract (`../worker/aiGateway.ts`; `../client/features/ai/AIChatPanel.tsx`). | High. One mock-provider browser path and 13 Worker gateway tests passed; real inference is unverified. |
| 5 | **Deeper file and presentation proof.** Exercise linked/large media and other font families, expand bounded Excalidraw import, and verify remote touchpad movement and broader transition animation before considering an accessible slide handout export. | Medium-high: protects portability and teaching use. | Current file adapter and frame order (`../client/features/portability/nativeBoard.ts`; `../client/features/presentation/`). | Medium-high. Multi-page and embedded-PNG round trips, light/dark first-slide captures, Safari two-frame navigation/direct short fading trail, a rapid three-frame navigation pass and 29 focused presentation tests pass; remote touchpad and broader transition appearance remain unverified; slide PDF/PPTX is not built. |

The installed SDK already exposes native shapes, frames, bindings, images/video, export helpers and snapshot APIs (inspected tldraw 5.4.2 package source: `src/lib/defaultShapeUtils.ts:1-28` and `src/lib/utils/export/exportAs.ts:11-50`; this package source is not distributed here). Those are useful foundations, not proof that every Excalidraw feature or every `ideas.md` concept is implemented. The immediate product test is whether users can make, revise and present diagrams faster while the board remains visually quiet.
